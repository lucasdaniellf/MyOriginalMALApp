<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myoriginalmalapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher_custom"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_custom_round"
        android:screenOrientation="sensorPortrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyOriginalMALApp">


        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.Home_Activity"
            android:theme="@style/Theme.MyOriginalMALApp.noActionBar" />
        <activity
            android:name=".activities.AnimePageActivity"
            android:theme="@style/Theme.MyOriginalMALApp.noActionBar" />
        <activity
            android:name=".activities.SearchAnimeActivity"
            android:theme="@style/Theme.MyOriginalMALApp.noActionBar" />

    </application>

</manifest>